<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.konrad.coursehub" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".SemesterListActivity"
            android:label="CourseHub" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CoursePagerActivity2"
            android:label="@string/title_activity_course_pager"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity android:name=".CoursePagerActivity"
            android:theme="@style/StyledIndicators">
        </activity>
        <activity android:name=".NewUnspecifiedEventActivity"
            />
    </application>

</manifest>
